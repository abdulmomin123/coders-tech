import styled from 'styled-components';

const Root = styled.div``;

const FeaturedProdBanner = () => {
  return (
    <Root>
      <h1>Prod banner</h1>
    </Root>
  );
};

export default FeaturedProdBanner;
